@model IEnumerable<QLKTXWEBSITE.Models.BedOfRoom>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/AdminQL/Views/Shared/Admin.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Create New</a>
</p>
<div class="row">
    @foreach (var item in Model)
    {
        <div class="col-md-3 mb-4">
            <div class="card">
                <div class="position-absolute" style="top: 10px; right: 10px;">
                    <a asp-action="Edit" asp-route-id="@item.BedId" class="btn btn-sm btn-primary">Edit</a>
                    <a asp-action="Delete" asp-route-id="@item.BedId" class="btn btn-sm btn-danger">Delete</a>
                </div>
                <img src="~/img/logogiuong.jpg" class="card-img-top" alt="Bed Image" style="width: 100%;">
                <div class="card-body">
                    <h5 class="card-title">Giường số: @Html.DisplayFor(modelItem => item.NumberBed)</h5>
                    @if (item.Status == false)
                    {
                        <a asp-controller="Students" asp-action="ListStudentBed" asp-route-id="@item.BedId" class="btn btn-warning btn-block">Chọn sinh viên</a>
                    }
                    else
                    {
                        <a asp-controller="Students" asp-action="Details" asp-route-id="@item.BedId" class="btn btn-primary btn-block">Thông tin sinh viên</a>
                    }
                </div>
            </div>
        </div>
    }
</div>
