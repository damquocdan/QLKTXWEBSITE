@model QLKTXWEBSITE.Models.Student

@{
    ViewData["Title"] = "Change Password";
}

<h1>@ViewData["Title"]</h1>

<div>
    <form asp-action="ChangePassword">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <input type="hidden" asp-for="StudentId" />
        <div class="form-group">
            <label asp-for="Password" class="control-label">Mật khẩu cũ:</label>
            <input type="password" asp-for="Password" class="form-control" placeholder="Nhập mật khẩu cũ" />
            <span asp-validation-for="Password" class="text-danger"></span>
        </div>

        <div class="form-group">
            <label asp-for="Password" class="control-label">Mật khẩu mới:</label>
            <input type="password" name="NewPassword" class="form-control" placeholder="Nhập mật khẩu mới"/>
            <span class="text-danger">@Html.ValidationMessage("NewPassword")</span>
        </div>

        <div class="form-group">
            <label asp-for="Password" class="control-label">Xác nhận mật khẩu mới:</label>
            <input type="password" name="ConfirmNewPassword" class="form-control" placeholder="Nhập lại mật khẩu mới" />
            <span class="text-danger">@Html.ValidationMessage("ConfirmNewPassword")</span>
        </div>

        <button type="submit" class="btn btn-primary">Đổi mật khẩu</button>
    </form>
</div>
<div>
    <a href="#" onclick="window.history.back();" class="link">Quay lại</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
